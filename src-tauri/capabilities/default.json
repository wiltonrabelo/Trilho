{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissões do Trilho (M1 + M2).",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "allow-get-app-info",
    "allow-list-commits-mock",
    "allow-repo-commands",
    "dialog:default"
  ]
}
